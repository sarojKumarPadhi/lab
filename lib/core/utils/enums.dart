enum MsgType { success, warning, failure, info }

enum PermissionExceptiontype {
  locationDenied,
  locationAlwaysDenied,
  locationServiceDenied,
  notificationDenied,
  notificationAlwaysDenied,
}
